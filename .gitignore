# SSH private keys and sensitive files
.ssh/id_rsa
.ssh/id_ed25519
.ssh/*_key
.ssh/*.key
.ssh/known_hosts*

# Environment and secret files
.env
.env.*
*.secret
*.secrets

# Authentication and certificates
*.pem
*.key
*.p12
*.pfx
*.crt
*.cert
*.ca-bundle

# API keys and tokens
api_keys/
tokens/
credentials/

# Machine-specific configs (not tracked)
.zshrc.local
.zshrc.work
.bashrc.local
.config/local/

# Convenience symlinks to secret files (ignored)
env.secrets
zshrc.local
zshrc.work
codex/config.local.toml

# Gemini CLI (sensitive, machine-specific files)
gemini/settings.local.json
gemini/oauth_creds.json
gemini/google_accounts.json
gemini/installation_id

# GitHub CLI secrets (kept in dotfiles but not tracked)
gh/hosts.yml
# gh/config.yml IS tracked (contains aliases, no secrets)

# GitLab CLI secrets (kept in dotfiles but not tracked)
glab/config.yml
# glab/aliases.yml IS tracked (contains aliases only)
# glab/config.template.yml IS tracked (template without secrets)

# macOS metadata
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Git local config overrides (machine-specific)
git/.gitconfig.local
